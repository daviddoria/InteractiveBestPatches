  // Find the patch with Id corresponding to 'value'
  /*
  unsigned int patchId = 0;
  for(unsigned int i = 0; i < this->PatchCompare.SourcePatches.size(); ++i)
    {
    if(this->PatchCompare.SourcePatches[i].Id == value)
      {
      patchId = i;
      break;
      }
    }
    
  Patch patch = this->PatchCompare.SourcePatches[patchId];
  */